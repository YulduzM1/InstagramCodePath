# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InstagramCodePath' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'AlamofireImage'
  # Pods for YOUR_APP
  pod 'Parse'
  pod 'MessageInputBar'

  # Pods for InstagramCodePath

  target 'InstagramCodePathTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InstagramCodePathUITests' do
    # Pods for testing
  end

end
